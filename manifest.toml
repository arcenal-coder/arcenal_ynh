#:schema https://raw.githubusercontent.com/YunoHost/apps/master/schemas/manifest.v2.schema.json

packaging_format = 2

id = "arcenal"
name = "ARCenal Agent"
description."en" = "Self-hosted AI agent (fork of Hermes Agent by Nous Research), served as a web app on your YunoHost server"
description."fr" = "Agent IA auto-hébergé (fork de Hermes Agent de Nous Research), servi comme application web sur votre serveur YunoHost"

version = "0.21.0~ynh14"

maintainers = ["compteonyx"]

[upstream]
license = "MIT"
website = "https://github.com/NousResearch/hermes-agent"
code = "https://github.com/NousResearch/hermes-agent"
cpe = "???" # not applicable — not a packaged CPE software

[integration]
yunohost = ">= 12.1.17" # Required by the current helpers 2.1 contract
helpers_version = "2.1"
architectures = ["amd64", "arm64"]
multi_instance = false
ldap = "not_relevant"
sso = "not_relevant"
disk = "2G"
ram.build = "1500M"
ram.runtime = "1G"

[install]
    [install.domain]
    type = "domain"

    [install.path]
    type = "path"
    default = "/arcenal"

    [install.admin]
    type = "user"

    [install.init_main_permission]
    type = "group"
    default = "admins"

    [install.provider]
    ask.en = "Choose the default model provider"
    ask.fr = "Choisissez le fournisseur de modèles par défaut"
    type = "select"
    choices = ["nous-portal", "openrouter", "openai", "anthropic", "custom"]
    default = "openrouter"

    [install.api_key]
    ask.en = "API key for the chosen provider (can be left empty and set later in the config panel)"
    ask.fr = "Clé API pour le fournisseur choisi (peut être laissée vide et définie plus tard dans le panneau de configuration)"
    type = "string"
    optional = true
    default = ""

    [install.model]
    ask.en = "Default model (e.g. openrouter/auto)"
    ask.fr = "Modèle par défaut (ex. openrouter/auto)"
    type = "string"
    default = "openrouter/auto"

[resources]
    [resources.system_user]
    allow_ssh = false
    allow_sftp = false
    home = "/var/www/arcenal"

    [resources.install_dir]
    dir = "/var/www/arcenal/app"

    [resources.data_dir]
    dir = "/var/www/arcenal/data"
    owner = "arcenal:rwx"
    group = "arcenal:rx"

    [resources.permissions]
    main.url = "/"
    main.auth_header = true
    main.protected = false

    [resources.ports]
    main.default = 9119

    [resources.apt]
    packages = ["python3", "python3-venv", "ripgrep", "ffmpeg", "git", "curl", "ca-certificates"]

    [resources.nodejs]
    version = "22.22"

    [resources.sources]
        [resources.sources.main]
        url = "https://github.com/arcenal-coder/arcenal-agent/archive/refs/tags/v0.21.0-arcenal5.tar.gz"
        sha256 = "663f9e983be31516aec8abc199e01bca754d123bc42627fa0200c92c3546c033"
