#!/bin/bash

# Sauvegarde d'ARCenal Agent avec le packaging YunoHost v2.

source /usr/share/yunohost/helpers

# Les sources sont retéléchargeables ; seules les données persistantes comptent.
ynh_backup "/var/www/$app/data"
